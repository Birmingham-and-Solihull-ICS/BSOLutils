# ICB Theme Palettes

This function returns a palette generated by colorRampPalette from one
of the ICB palettes

## Usage

``` r
icb_theme_pal(
  palette = c("cluster", "bsol", "alternative", "ics_blue", "ics_orange", "ics_green",
    "ics_purple", "ics_navy"),
  reverse = FALSE,
  ...
)
```

## Arguments

- palette:

  The name of palette to use. 'cluster' is the BSOL / BC cluster colours
  and default.

- reverse:

  Optional: boolean value to reverse the palette

- ...:

  arguments to pass to colorRamp

## Value

A colorRamp function

## Examples

``` r
icb_theme_pal("cluster")
#> function (n) 
#> {
#>     x <- ramp(seq.int(0, 1, length.out = n))
#>     if (ncol(x) == 4L) 
#>         rgb(x[, 1L], x[, 2L], x[, 3L], x[, 4L], maxColorValue = 255)
#>     else rgb(x[, 1L], x[, 2L], x[, 3L], maxColorValue = 255)
#> }
#> <bytecode: 0x5616bc6128b0>
#> <environment: 0x5616bcabe780>
icb_theme_pal("bsol")
#> function (n) 
#> {
#>     x <- ramp(seq.int(0, 1, length.out = n))
#>     if (ncol(x) == 4L) 
#>         rgb(x[, 1L], x[, 2L], x[, 3L], x[, 4L], maxColorValue = 255)
#>     else rgb(x[, 1L], x[, 2L], x[, 3L], maxColorValue = 255)
#> }
#> <bytecode: 0x5616bc6128b0>
#> <environment: 0x5616bcb0c3d8>
# or, specify a palette
icb_theme_pal("ics_orange")
#> function (n) 
#> {
#>     x <- ramp(seq.int(0, 1, length.out = n))
#>     if (ncol(x) == 4L) 
#>         rgb(x[, 1L], x[, 2L], x[, 3L], x[, 4L], maxColorValue = 255)
#>     else rgb(x[, 1L], x[, 2L], x[, 3L], maxColorValue = 255)
#> }
#> <bytecode: 0x5616bc6128b0>
#> <environment: 0x5616bcb66510>
# a reverse palette
icb_theme_pal("cluster", TRUE)
#> function (n) 
#> {
#>     x <- ramp(seq.int(0, 1, length.out = n))
#>     if (ncol(x) == 4L) 
#>         rgb(x[, 1L], x[, 2L], x[, 3L], x[, 4L], maxColorValue = 255)
#>     else rgb(x[, 1L], x[, 2L], x[, 3L], maxColorValue = 255)
#> }
#> <bytecode: 0x5616bc6128b0>
#> <environment: 0x5616bcbc2168>
```
